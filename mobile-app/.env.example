# FastAPI (yerel): kök URL — şema + host + port, path YOK.
# Fiziksel telefon / tablet: bilgisayarının Wi‑Fi IP’si (127.0.0.1 telefonda yanlış adres).
# Örnek: ip -brief a  veya  hostname -I
EXPO_PUBLIC_API_BASE_URL=http://192.168.x.x:8000

# Eski projelerde kalan EXPO_PUBLIC_API_URL satırını .env içinden SİL (artık kullanılmıyor; yanlışlıkla 127.0.0.1 bırakılırsa kafa karıştırır).

# İsteğe bağlı (nadiren): /api/v1 tam kök — apiService artık bunu kullanmıyor; sadece BASE_URL için yeterli:
# EXPO_PUBLIC_API_BASE_URL yeterli.

EXPO_PUBLIC_SUPABASE_URL=https://<your-project-ref>.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=<your-supabase-anon-key>
# Opsiyonel: sifre sifirlama mailindeki redirect (dev build / tek sema). Bos = Expo Go exp://...
# EXPO_PUBLIC_PASSWORD_RESET_REDIRECT=mobileapp://reset-password
